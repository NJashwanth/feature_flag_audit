name: flutter_sample_app
description: Sample app used by feature_flag_audit example.
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  feature_flag_audit:
    path: ..

flutter:
  uses-material-design: true